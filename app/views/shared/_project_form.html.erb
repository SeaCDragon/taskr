<%= form_for(@project) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :title, placeholder: "name a new project" %>
	</div>
	<%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
